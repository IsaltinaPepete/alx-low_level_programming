int add(int i, int k);
int sub(int i, int k);
int mul(int i, int k);
int div(int i, int k);
int mod(int i, int k);

/**
* add - adds two integers
* @i: first integer to add
* @k: second integer to add
* Return: the sum
*/
int add(int i, int k)
{
	return (i + k);
}

/**
* add - sub two integers
* @i: first integer to sub
* @k: second integer to sub
* Return: the sub
*/
int sub(int i, int k)
{
	return (i - k);
}

/**
* add - mul two integers
* @i: first integer to mul
* @k: second integer to mull
* Return: the mul
*/
int mul(int i, int k)
{
	return (i * k);
}

/**
* add - adds two integers
* @i: first integer to add
* @k: second integer to add
* Return: the div
*/
int div(int i, int k)
{
	return (i / k);
}

/**
* add - adds two integers
* @i: first integer to add
* @k: second integer to add
* Return: the sum
*/
int mod(int i, int k)
{
	return (i % k);
}
